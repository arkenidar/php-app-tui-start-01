
# netcat

- in msys install it with :
    ```bash
    pacman -S gnu-netcat
    ```

- get help with :
    ```bash
    nc --help
    ```

- connect to server ( after launching ) :
    ```bash
    nc localhost 8080
    ```